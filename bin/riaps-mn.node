#!/bin/sh
# Set up a virtual host 
# To be run 'non-lead' nodes 

# Add default gateway (mininet)
route add default gw 10.0.0.1

# Change to (non-privileged) user
echo "source setup.node; python3 src/deplo.py" | sudo -s -u riaps 
